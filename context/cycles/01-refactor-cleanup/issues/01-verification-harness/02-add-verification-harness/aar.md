1. Did it go as planned? Yes -- implemented a repository-local verification harness at `scripts/check` and validated it against direct parse commands.
2. What changed from the sub-issue plan: Added explicit dependency and file-presence guards so missing prerequisites fail early with clear errors.
3. Carry-forward -- flags to write in the parent, divergence to note for future siblings, notes for the parent issue's AAR: No new flags; parent issue flags for command path and explicit-file scope were resolved in this sub-issue.
